import React from "react";
import { ClipLoader } from "react-spinners";

const LoadingSpinner: React.FC = () => {
  return <ClipLoader size={30} color="#00BFFF" />;
};

export default LoadingSpinner;
